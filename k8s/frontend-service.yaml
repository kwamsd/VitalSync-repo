apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: vitalsync-frontend
spec:
  type: ClusterIP
  selector:
    app: vitalsync-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
